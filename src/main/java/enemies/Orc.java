package enemies;

import com.sun.org.apache.xpath.internal.axes.ChildTestIterator;
import enemies.Enemy;

public class Orc extends Enemy {

    public Orc(int healthPoints, int hitPoints) {
        super(healthPoints, hitPoints);
    }
}
